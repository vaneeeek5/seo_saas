name: Deploy Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Linting and Type Checking
        run: |
          npm run lint --if-present
          npm run build --if-present

      - name: Build Docker Image
        run: docker build -t my-seo-app:latest .

      - name: Deploy Placeholder
        run: |
          echo "Deploying to server via SSH / Cloud Run..."
          # Example SSH Deploy (uncomment and configure secrets)
          # mkdir -p ~/.ssh
          # echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          # chmod 600 ~/.ssh/id_rsa
          # ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts
          # ssh user@${{ secrets.SERVER_HOST }} 'cd /opt/app && git pull && docker-compose up -d --build'
